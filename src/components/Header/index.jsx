import React from 'react';
import PropTypes from 'prop-types';
header.propTypes = {
    
};

function header(props) {
    return (
        <div>
            
        </div>
    );
}

export defaultheader;